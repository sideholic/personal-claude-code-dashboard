# Absolute path to the plugin's event log inside `.claude-team`.
# The board derives the `.claude-team` root from this path's directory and reads
# the ticket folder (tickets/<status>/*.md) directly — read-only.
EVENTS_LOG=/Users/you/develop/workspace/personal-claude-code-v2/.claude-team/events.jsonl
